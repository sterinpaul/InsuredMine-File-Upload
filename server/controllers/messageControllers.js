import messageHelpers from "../helpers/userHelpers.js"


const messageControllers = {
    addMessage:async(req,res)=>{
        const { message, day, time } = req.body
    }
}

export default messageControllers